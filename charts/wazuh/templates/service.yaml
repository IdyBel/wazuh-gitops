apiVersion: v1
kind: Service
metadata:
  name: {{ include "wazuh.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "wazuh.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
